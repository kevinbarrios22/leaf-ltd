# === Base de datos ===
DB_PASSWORD=postgres

# === JWT ===
JWT_SECRET=clave-segura-de-32-caracteres-o-mas

# === CORS (orígenes separados por coma) ===
CORS_ORIGINS=http://localhost:5173,http://localhost:8080

# === Puerto del servidor ===
PORT=8080
